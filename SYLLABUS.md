# Rust で数値計算 — シラバス

## この文書の位置づけ

`vini-fda/numerical-algorithms-rust` の fork は取りやめ、自前のリポジトリで進める。理由は以下（調査結果）:

- 最終 push 2023-12-28、コミット10本すべて4日間、以後停止。star 2 / fork 0
- 全54ファイル中、中身のあるファイルは4本のみ。残り48ファイルは10〜53バイト＝`# 見出し` 1行だけ
- `Cargo.toml` が無く `.rs` ファイルが1つも無い。"in Rust" を名乗るリポジトリに Rust コードがゼロ

fork で得られるのは実質 SUMMARY.md（標準的な数値解析教科書の目次）のみで、引き継ぐ価値がない。

## 進め方の規約

実装は自分でやる。Claude は本文を書かない。TDD で進める——先にテストを書いて赤にしてから実装する。

Claude の役割は4つ:

| 役割 | 具体 | やらないこと |
|---|---|---|
| オラクル | 期待値を出す。scipy / Octave / 解析解と突き合わせる | 受け入れテストを先に書く（＝関数シグネチャを確定させる） |
| 一次情報の確認 | docs.rs・クレートの examples・原論文で「今の作法」を裏取り | 記憶で API を答える |
| レビュー | 誤差評価の妥当性、API が ecosystem の慣用に沿っているか | 実装の代筆 |
| ヒント | ZPD の範囲でヒント | 答えとコード片 |

**受け入れテストを Claude が書かない理由**: テストを書くと関数シグネチャと分解の仕方が確定してしまう。数学ではなく Rust の設計判断が唯一の未知なので、そこを先に埋めると学習が消える。自分で設計して外した経験だけが知見として残る。

**オラクルは受け入れテストとは別物**: 「この入力での正解は 0.7390851332151607」は答えを教えるが、`fn newton(...)` がどういう型を取るべきかは何も決めない。ここは Claude が出しても学習は壊れない。

### オラクルの取り方

外部依存をテストに持ち込まない。scipy 等で事前に値を生成し、定数としてテストに埋める。テスト実行時に Python を呼ばない。

- Python 経路: `uv run --with scipy python -c '...'`（環境を汚さない。ただし現時点で `uv` は未インストール。必要になった時点で入れる）
- 解析解が取れる問題を優先する（オラクル不要になるので最良）
- 相互検証: 自作 FFT ⇔ `rustfft`、自作 LU ⇔ `faer`、自作 autodiff ⇔ 数値微分

## リポジトリ構成

単一 crate + モジュール。workspace には分けない。章ごとに crate を切るのは YAGNI で、依存も章ごとに増えるだけ。

```
numerics/
├── Cargo.toml             # edition 2024（rustc 1.96 で stable）
├── book/                  # 解説（mdBook）。章ごとに1ファイル
├── src/
│   ├── lib.rs
│   ├── float.rs           # 浮動小数点と丸め
│   ├── roots.rs           # 根の探索
│   ├── linalg/            # 密行列の線形代数・固有値問題（大きくなるのでディレクトリ）
│   ├── interp.rs          # 補間と近似
│   ├── quad.rs            # 求積
│   ├── ode.rs             # 常微分方程式
│   ├── optim.rs           # 最適化
│   ├── autodiff/          # 自動微分
│   ├── fft.rs, mc.rs      # FFT と Monte Carlo
│   └── pde/               # PDE の差分法
├── examples/              # 収束次数のプロット等、動かして見るもの
└── benches/               # criterion。密行列の線形代数から先で必要になったら
```

依存は必要になった章で足す。最初は `approx` だけでよい。

### テスト基盤（90日 DL で確認済み・デファクト）

| クレート | 90日 DL | 用途 |
|---|---|---|
| `approx` 0.5.1 | 30.6M | 数値 assert（`assert_relative_eq!` / `assert_abs_diff_eq!`） |
| `proptest` 1.11 | 43.1M | 性質ベーステスト。数値計算と相性が良い |
| `criterion` 0.8.2 | 54.1M | ベンチ。密行列の線形代数から先 |

`proptest` の使いどころが数値計算では特殊で重要: 「答え」ではなく「不変量」を検査する。例——求めた根を $f$ に入れたらゼロ近傍、$PA = LU$ の再構成、求積作用素の線形性、FFT の逆変換で元に戻る（往復テスト）、autodiff の結果が数値微分と一致。個別の入力に対する正解を用意せずに広い入力空間を叩ける。

---

# 章立て

各章は「題材 / Rust の設計判断（本体）/ 数値的な復習ポイント / オラクル / クレートの扱い / 完了条件」で構成する。

---

## 浮動小数点と丸め

唯一の前置き。全章に効くのでここだけは先にやる。薄く。

**題材**: IEEE 754 binary64（53bit 仮数、$\varepsilon = 2^{-52} \approx 2.22\times10^{-16}$）、桁落ち（catastrophic cancellation）、補償付き総和（Kahan / Neumaier）。

**Rust の設計判断**:
- `f64` の比較をどう扱うか。`PartialOrd` しか無い（`Ord` が無い）のはなぜか、`total_cmp` は何を解決するか
- NaN の伝播を型で防ぐか、実行時に弾くか

**数値的な復習ポイント**:
- 二次方程式 $ax^2+bx+c=0$ の根の公式が、$b^2 \gg 4ac$ のとき片方の根で桁落ちする。回避は $x_1 x_2 = c/a$ を使う
- 浮動小数点の加算は結合的でない。したがって `rayon` で総和を並列化すると、スケジューリング次第で結果のビットが変わる。「同じコードが同じ答えを返さない」問題は数値解析の教科書には出てこないが、実務では必ず出る（← 科学技術計算の議論から拾った追加項目）

**オラクル**: 解析的に自明。桁落ちは素朴な式と安定な式を両方書いて差を見る。

**完了条件**: Kahan 加算が素朴な総和より精度で勝つことを、実際に有意な差が出る入力（例: 大きな値に微小値を大量に足す）で示せる。

---

## 根の探索

**題材**: 二分法、Newton–Raphson、割線法、不動点反復。

**Rust の設計判断（この章の本体）**:
- 関数をどう受けるか。`f: impl Fn(f64) -> f64` / `&dyn Fn(f64) -> f64` / トレイトを定義する——単相化によるコード膨張とコンパイル時間、動的ディスパッチのコスト、API の使い勝手のトレードオフ
- スカラーを `f64` 固定にするか、`num_traits::Float` で generic にするか。generic 化の代償（リテラルが書けない、`T::from(2.0).unwrap()` が散る）は実際に払ってみないと分からない
- **導関数をどう渡すか**——別引数のクロージャ / 数値微分 / dual number。3番目は自動微分の伏線
- 停止条件: $|f(x_n)|$ か $|x_{n+1}-x_n|$ か相対誤差か。それぞれ壊れる場合があるので、どれを選ぶかは設計判断
- 失敗の型: `Result<f64, E>` の `E` に何を入れるか。「収束しなかったが最善の推定値はこれ」を返したいなら `Result` は正しい形か

**数値的な復習ポイント**:
- 収束次数: 二分法 1次（線形）、Newton 2次、割線法 $\varphi = (1+\sqrt5)/2 \approx 1.618$
- Newton が2次収束するのは**単根**（$f'(x^*) \neq 0$）のとき。重根では1次に落ちる
- 二分法だけが収束を保証する（中間値定理）。ここが「速いが保証がない」Newton との本質的な対比で、実用のソルバー（Brent 法）が両者を混ぜる理由

**オラクル**:
- $\cos x = x$ の不動点（Dottie number）= `0.7390851332151607`（f64 で検証済み: この値で `cos(x) - x` が厳密に 0.0）
- 多項式なら解析解

**クレートの扱い**: `roots` クレートは 90日 DL 0.35M だが最終更新 2022-12-21 で実質停止。自作が正当な領域。

**完了条件**: 収束次数を実測して理論値と一致させる（$\log|e_{n+1}|$ vs $\log|e_n|$ の傾き）。重根で Newton が2次を失うことも実測で示す。

---

## 密行列の線形代数 & 固有値問題 & 補間と近似

この章が Rust 的には本丸。最も時間をかける。**題材**: LU 分解（部分ピボット選択）、QR 分解（Householder）、SVD、Cholesky、反復法（Jacobi / Gauss–Seidel / 共役勾配法）。固有値問題はべき乗法・QR 法・Lanczos、補間と近似は Lagrange / Newton / Chebyshev / スプラインを扱う。補間と近似は求積の前提。

**Rust の設計判断**:
- **クレート選定**: `ndarray` 0.17（23.7M）/ `nalgebra` 0.35（15.2M）/ `faer` 0.24（1.4M）。ndarray = NumPy 相当の n 次元配列、nalgebra = 固定サイズ・幾何寄り、faer = pure Rust の高性能密行列。この選択自体が最初の課題。まず自前の最小 `Matrix` 型を書いてから既製品に移ると、何が解決されているかが分かる
- **in-place 分解**（`&mut self`）vs 新規確保。LU は入力行列を破壊して同じメモリに L と U を詰めるのが古典的な作法（対角の 1 は暗黙）。これを Rust の借用規則の中でどう表現するか
- **ビューとスライス**: 部分行列を借用として渡す。`ndarray` の `ArrayView` / `ArrayViewMut` が何を解決しているか。行の交換を「データを動かす」か「置換ベクトルを持つ」か
- 行優先 / 列優先（LAPACK は列優先）の選択と、キャッシュ効率

**数値的な復習ポイント**:
- 条件数 $\kappa(A) = \|A\|\|A^{-1}\|$、後退安定性（backward stability）。Wilkinson の「計算した解は、わずかに摂動した問題の厳密解である」という枠組み
- 最小二乗を正規方程式 $A^\top A x = A^\top b$ で解いてはいけない。$\kappa(A^\top A) = \kappa(A)^2$ で条件数が二乗される。QR か SVD を使う
- Cholesky と正定値性。推定した共分散行列が数値的に半正定値にならず Cholesky が落ちるのは金融で有名な実害。ガウス過程で対角に jitter を足すのと同じ問題（統計・ML がここに落ちる）
- 逆行列は作らない。$A^{-1}b$ ではなく $Ax=b$ を解く

**オラクル**: scipy（`scipy.linalg.lu`, `qr`, `svd`, `cholesky`）で生成して埋め込む。相互検証として `faer` の結果と突き合わせる。Hilbert 行列は条件数が悪いことで有名なので、条件数の実験に使える。

**完了条件**: Hilbert 行列で、正規方程式版と QR 版の最小二乗の精度差を実測して見せる。

---

## 求積（数値積分）

**題材**: 台形則、Simpson 則、Richardson 外挿 → Romberg 積分、Gauss–Legendre、適応分割。

**Rust の設計判断**:
- 高階関数の受け方（根の探索の再訪だが、今度は「積分器を返す」形もある）
- **適応分割の再帰と所有権**。再帰で区間を分割していく際、結果をどう畳み込むか。明示スタックに書き換えられるか
- イテレータで書き切れるか。`Iterator::sum()` で済むのか、Kahan 加算（浮動小数点と丸め）が要るのか

**数値的な復習ポイント**:
- 誤差次数: 合成台形則 $O(h^2)$、合成 Simpson 則 $O(h^4)$
- Simpson 則は2次多項式で補間しているのに3次多項式まで厳密。対称性による1次分の得（superconvergence）
- Gauss–Legendre $n$ 点は $2n-1$ 次まで厳密。節点を自由に選べる分だけ次数が倍近く上がる
- Richardson 外挿は「誤差の主要項を打ち消す」という汎用の技法で、常微分方程式の埋め込み型 RK にも数値微分にも効く

**オラクル**: 解析解が取れる被積分関数を選ぶ（オラクル不要が最良）。特異点付き・振動する被積分関数で適応分割の必要性を出す。`scipy.integrate.quad`。

**完了条件**: 誤差次数を実測（$\log(\text{error})$ vs $\log h$ の傾きが $+2$, $+4$。$\log n$ 横軸なら $-2$, $-4$）。Gauss–Legendre が $2n-1$ 次で機械精度、$2n$ で崩れることを示す（$n \lesssim 12$ に限る）。

---

## 常微分方程式（ODE） & 確率微分方程式（SDE）

**題材**: Euler 法、RK4、埋め込み型 RK（Dormand–Prince）による適応刻み、陰的解法（後退 Euler / 台形則）、stiff 方程式。追加で SDE: Euler–丸山法、Milstein 法。

**Rust の設計判断**:
- **状態型に対する generic**。スカラーの ODE と連立 ODE を同じコードで扱うには、状態を `Vec<f64>` にするか、`[f64; N]` で const generics にするか、`nalgebra::SVector` にするか、独自トレイト（加算とスカラー倍ができれば十分＝ベクトル空間）を切るか。「必要な演算だけをトレイトで要求する」という Rust の設計が最も綺麗に効く題材
- 右辺 $f(t, y)$ を closure にするか trait にするか。状態を持つ右辺（パラメータ付き）をどう扱うか
- 適応刻みの結果をどう返すか。`Vec<(t, y)>` を貯めるか、`Iterator` として遅延生成するか。後者は Rust らしいが、刻み幅制御と組み合わせると設計が難しい
- 陰的解法は内側で非線形方程式を解く＝根の探索の Newton 法をここで再利用する。モジュール境界の設計が問われる

**数値的な復習ポイント**:
- RK4 は4次精度で1ステップに4回評価。ただし **Butcher の障壁**により5次には6回必要——「次数＝評価回数」は4次までしか成り立たない
- **A-安定性**: 陽的 RK は安定領域が有界なので原理的に A-安定になれない。stiff 方程式に陽解法を使うと、精度ではなく安定性が刻み幅を縛る
- **Dahlquist の第2障壁**: A-安定な線形多段法の次数は2以下で、最小誤差定数を持つのは台形則
- SDE の収束は2つに割れる。Brown 運動が $\sqrt{\Delta t}$ でスケールするため通常の Taylor 展開の次数の議論が壊れ、**強収束**（軌道の一致）と**弱収束**（分布・期待値の一致）が別々の次数を持つ。Euler–丸山は強 0.5 / 弱 1.0、Milstein は強 1.0。「収束次数」という概念自体が変質するので、常微分方程式の理解を試す良い題材

**オラクル**: 解析解のある系（線形 ODE、調和振動子、Van der Pol は解析解無しだが参照解を高精度で作れる）。エネルギー保存系ではエネルギー誤差を見る。SDE は幾何ブラウン運動が解析解を持つので、強収束・弱収束の次数を両方実測できる。

**クレートの扱い**: `ode_solvers`（90日 0.21M）が実在する。先にその API 設計を読んでから自作する——「Rust で ODE ソルバーを抽象化するとどうなるか」の既存解を見てから自分の設計と比較する。

**完了条件**: 収束次数の実測（強・弱の両方を SDE で）。stiff 方程式で陽解法が破綻し陰解法が通ることを実測。

---

## 最適化

**題材**: 勾配降下、Newton 法、準 Newton（BFGS / L-BFGS）、直線探索（Armijo / Wolfe 条件）、信頼領域。制約付きは軽く（ラグランジュ乗数、射影勾配）。

**Rust の設計判断**:
- `argmin`（90日 0.79M）の trait 設計を読む。目的関数・勾配・ヘッセ行列をどう抽象化しているか。これは「Rust で数値アルゴリズムを抽象化する」実例の教科書として読む価値がある
- L-BFGS の履歴（過去 $m$ ステップの $s_k, y_k$）をどう持つか。リングバッファ、`VecDeque`
- 勾配を「関数として受ける」か「トレイトのメソッド」か。自動微分と接続する境界をどこに置くか

**数値的な復習ポイント**:
- Newton 法（根の探索）の多変数版。ヘッセ行列が正定値でないときの修正
- BFGS の更新式が正定値性を保つ条件（曲率条件 $s^\top y > 0$）
- **決定論的最適化と確率的最適化の哲学的な断絶**: 数値解析の最適化は「収束させる」ものだが、SGD は本質的に収束させない（勾配のノイズが正則化として働く）。同じ「最適化」の看板で、目的関数が確定的か期待値かで作法が真逆になる。数値解析だけを追うと見えない対比
- 最尤推定はそのまま最適化（統計がここに落ちる）

**オラクル**: Rosenbrock 関数（最小値 $(1,1)$ で 0）、Himmelblau など標準テスト関数。`scipy.optimize.minimize`。

**完了条件**: Rosenbrock で最急降下・Newton・BFGS の反復回数を比較し、条件数の悪い谷で最急降下がジグザグする様子を実際に出す。

---

## 自動微分（新設）

古典的な数値解析の canon には無いが、現代の数値計算の本質。根の探索と PDE の差分法の橋になる。

**題材**: 前進モード（dual number）、逆モード（テープ / 計算グラフ）。

**Rust の設計判断（密度が最も高い）**:
- **前進モード**: `Dual { re: f64, du: f64 }` に `impl Add / Mul / Neg` と `sin`/`exp` 等を実装。演算子オーバーロードだけで微分が「勝手に」伝播する。根の探索で「導関数をどう渡すか」を保留した選択肢の回収——`f: impl Fn(Dual) -> Dual` にすれば導関数を別途渡す必要がなくなる
- **逆モード**: 計算グラフの所有権をどう持つか。ここが Rust で最も難しい部分で、素朴に `Rc<RefCell<Node>>` に逃げるか、**インデックスを持つ arena / テープ方式**にするか。後者が Rust の定石で、有限要素法のメッシュとまったく同じ問題
- グラフを持つデファクトの道具は確立している: `petgraph` 97.8M / `slotmap` 23.6M / `typed-arena` 14.2M（90日 DL）。自作 autodiff と有限要素法のメッシュを同じ道具で解ける——汎用の道具で複数の問題を解く形

**数値的な復習ポイント**:
- 前進モードのコストは入力次元 $n$ に比例、逆モードは出力次元 $m$ に比例。$f: \mathbb{R}^n \to \mathbb{R}$（＝損失関数）では逆モードが圧倒的に有利。backprop が逆モードである理由はこれだけ
- 数値微分（差分）が使えない理由: 打ち切り誤差と丸め誤差のトレードオフで、最適な $h$ でも精度は $\sqrt{\varepsilon} \approx 10^{-8}$ 程度に留まる。autodiff は**機械精度**で導関数を出す
- 記号微分との違い（式の膨張がない）

**オラクル**: 数値微分（中心差分）との一致を性質テストで。解析的に微分できる関数（多項式、`sin`/`exp` の合成）で厳密一致を確認。

**クレートの扱い**: デファクトが無い。`num-dual` 87k（前進モードのみ）、`autodiff` 33k は 2023-03 で停止。自作が正当。

**完了条件**: 前進モードで根の探索の Newton 法を導関数の手渡しなしに動かす。逆モードで多変数関数の勾配を1回の逆伝播で得て、数値微分と一致させる。

---

## FFT と Monte Carlo

**題材**: 離散 Fourier 変換、Cooley–Tukey FFT、Monte Carlo 積分、分散削減、準乱数（Sobol / Halton 列）。

**Rust の設計判断**:
- `num-complex`（72.7M）の `Complex<f64>` を使うか、実部・虚部を別配列（SoA）にするか。SIMD 化を考えるなら後者という、データレイアウトの古典的トレードオフ
- 再帰的 Cooley–Tukey と反復版（bit-reversal permutation）。in-place にできるか
- `rustfft`（6.2M）との相互検証。プラン（事前計算）を持つ API がなぜそうなっているか
- 乱数: `rand` 0.10（374M）。**RNG をどう注入するか**——グローバル状態を避け、シードを引数で受けて再現性を確保する

**数値的な復習ポイント**:
- DFT $O(N^2)$ → FFT $O(N \log N)$。2の冪でない長さの扱い（Bluestein / mixed-radix）
- Monte Carlo 積分の収束は $O(N^{-1/2})$ で、次元に依らない。これが高次元で MC が勝つ理由
- 準乱数の収束は $O((\log N)^d / N)$。理論的な根拠は **Koksma–Hlawka 不等式**: 誤差 $\le V(f) \cdot D_N^*$（関数の変動 × 星型判別数）。「ランダムでないほうが速い」ことに明確な数学的理由がある。金融のモンテカルロでは標準（← 科学技術計算の議論から拾った追加項目）
- 分散削減: 対称変量、制御変量、重点サンプリング

**クレートの扱い**: `rustfft` は自作の検証用に使う。準乱数は `sobol_burley` 16k と弱いので自作寄り。

**完了条件**: 自作 FFT が `rustfft` と機械精度で一致。往復テスト（IFFT∘FFT = 恒等）を proptest で。MC と QMC の収束次数を実測し、次元を上げたときの優劣の逆転を見る。

---

## PDE の差分法 & 有限要素法

終着点。Rust の設計緊張が全題材で最大。

**題材**: 差分法（熱方程式・波動方程式・Poisson 方程式）、線の方法、有限要素法（弱形式、Galerkin、1次元→2次元三角形要素）。

**Rust の設計判断**:
- **疎行列**: CSR / CSC / COO。密行列とは別のデータ構造で、「配列の所有権」ではなくインデックス構造の所有権を設計することになる。クレートは分散していて集約していない——`sprs` 1.5M / `nalgebra-sparse` 0.16M / `faer` 本体の `sparse` モジュール（solvers 込み。単体の `faer-sparse` クレートは 2024-02 で止まり本体に統合済み）
- **メッシュのデータ構造**: 要素↔節点↔辺の相互参照＝グラフ。Rust で最も設計が難しいとされる領域そのもの。`Rc<RefCell<>>` に逃げるか、インデックス / arena 方式にするか——自動微分のテープと同じ問題で、同じ道具（`slotmap` / `typed-arena` / `petgraph`）が使える
- **剛性行列のアセンブリの並列化**: 要素ループを `rayon`（109.5M）で並列化すると、共有 DOF への加算で競合する。Rust の型システムが「不可能な並列化を弾く」ことを実感する最良の題材。逃げ方（要素ごとに局所行列を作って後で reduce、色分け、`Mutex` 分割）の選択が設計判断
- **matrix-free**: `faer` に `matrix_free` モジュールがあり、行列を陽に持たず作用素を trait で抽象化する実例が読める。共役勾配法（密行列の線形代数）は行列そのものではなく行列ベクトル積しか要求しない——その事実をトレイトで表現する

**数値的な復習ポイント**:
- **von Neumann（フォン・ノイマン）安定性解析** = Fourier 安定性解析。差分スキームの誤差を Fourier モードに分解し、増幅率 $|g(k)| \le 1$ を要求する
  - **用語の注意**: 「von Neumann 安定性解析」は John von Neumann（ハンガリー、20世紀）、「Neumann 境界条件」は Carl Neumann（ドイツ、19世紀）。別人である。PDE 章では両方同時に出てくるので混ざりやすい
- **CFL 条件**（Courant–Friedrichs–Lewy）。陽的差分の熱方程式では $r = \Delta t / \Delta x^2 \le 1/2$。時間刻みが空間刻みの二乗で縛られるのが陰解法を使う動機
- **Lax の同値定理**: 適切な線形初期値問題において、適合性（consistency）+ 安定性（stability）⟺ 収束（convergence）。「離散化が正しい」と「安定」は別物で、両方要る
- FEM: 弱形式への変換（部分積分で微分の階数を下げる）、Galerkin 法、基底関数の局所台が疎性を生む

**クレートの扱い**: Rust に FEM のデファクトは存在しない。`fenris` は 90日 242 DL / 最終更新 2023 で事実上停止。既製品を呼ぶ選択肢が無く、自分で書くのが唯一の道。

**完了条件**: 熱方程式の陽解法で $r > 1/2$ にしたときに発散し、von Neumann 解析の予測と一致することを実測。1次元 Poisson を FEM で解いて解析解と一致。2次元三角形要素まで到達できれば十分。

---

# 元の目次から落としたもの

| 落とした項目 | 理由 |
|---|---|
| Mathematical Preliminaries（付録） | 既知 |
| Importance of Numerical Methods | 読み物 |
| Errors / Stability の独立章 | 「安定性」は3つあり、名前が同じだけの別概念（数値線形代数の後退安定性 / ODE の A-安定性 / PDE の von Neumann 安定性）。1章に括ると混同を助長する。各章の入口で必要な分だけ復習する JIT 形にした |
| 統計の独立章 | 統計の計算的な核は既に密行列の線形代数（最小二乗・Cholesky）、最適化（MLE）、FFT と Monte Carlo に入っている。純粋に統計側の部分（分布関数、検定）は `statrs` 8.5M を呼ぶ話でアルゴリズムを書く題材にならない |
| 機械学習の独立章 | 古典的 ML は線形代数＋最適化の応用（線形回帰→QR/SVD、ロジスティック回帰→Newton、PCA→SVD、SVM→QP、GP→Cholesky）。唯一 canon に無いのが自動微分なので、そこだけ独立した章にした |
| 深層学習の実装 | Rust の DL は `candle-core` 2.5M / `tch` 1.2M（libtorch バインディング）/ `burn` 0.38M に割れ、合計しても `ndarray` 単体 23.7M に遠く及ばない。デファクトが無いのではなく人が少ない。`dfdx` は 4k / 最終更新 2023-07 で死亡（const generics で tensor の shape を型に載せる設計が定着しなかった）。深入りすると GPU カーネルとメモリ管理の学習に変質する。自作するのは autodiff エンジンまでという線を引く |
| GPU / MPI | `mpi` 90日 39k で Rust では実質使われていない。GPU は cudarc 2.8M / wgpu 9.1M どちらでもカーネル記述の学習が本体になり、数値解析から離れる |
| 精度保証付き数値計算（区間演算） | `inari` 90日 1.1k。興味があれば単発で触る程度 |

---

# 着手手順（根の探索）

決めること・オラクル・完了条件は[根の探索](book/src/ch01-roots.md)にある。ここはリポジトリ側の作業順だけを書く。

1. `src/roots.rs` を作って `lib.rs` に `mod` を足す（crate と dev-deps は作成済み）
2. 先に赤いテストを書く——値は本のオラクル表から取る
3. 二分法から実装（収束が保証されるので、他の手法の参照解にも使える）
4. Newton → 割線 → 不動点反復
5. 収束次数の実測を `examples/` に書く

---

# 未決事項

## [未決] 密行列の線形代数で使う行列クレートの選定

`ndarray` / `nalgebra` / `faer` の三択。密行列の線形代数の最初の課題そのものとして保留する。まず自前の最小 `Matrix` 型を書いてから既製品に移り、何が解決されているか（ビュー、ストライド、ブロードキャスト、BLAS への委譲）を、自分で不便を踏んでから見る。決める前に下の地形データを取り直すこと。

## [未決] scipy オラクル用の `uv` 導入

根の探索は解析解で足りるので不要。密行列の線形代数から先で scipy が本格的に要る。

- 現時点で `uv` は未インストール（`which uv` で確認済み）
- Python 環境管理は uv で確定していて、その「必要時」がここで来る
- 環境を汚さない実行形: `uv run --with scipy python -c '...'`
- 導入は自分の手で

---

# クレート地形の実測（2026-08-08 時点）

この表がクレート判断の一次データ。本文中の個々の言及はここから導出したもの。crates.io API の `recent_downloads`（直近90日）と GitHub API で実測。faer のモジュール構成は docs.rs で確認。

**陳腐化に注意**——数値は動く。密行列の線形代数のクレート選定など重い判断の前には取り直すこと。

| クレート | 90日 DL | ver | 最終更新 | 位置づけ |
|---|---|---|---|---|
| `num-traits` | 188M | 0.2.19 | 2024-05 | 事実上インフラ |
| `num-complex` | 72.7M | 0.4.6 | 2025-01 | 同上 |
| `rayon` | 109.5M | 1.12.0 | 2026-04 | 有限要素法の並列アセンブリ |
| `petgraph` / `slotmap` / `typed-arena` | 97.8M / 23.6M / 14.2M | — | — | **グラフ所有権のデファクト**。自動微分・有限要素法で共用 |
| `ndarray` | 23.7M | 0.17.2 | 2026-01 | NumPy 相当。密行列の線形代数の候補 |
| `nalgebra` | 15.2M | 0.35.0 | 2026-05 | 固定サイズ・幾何寄り。密行列の線形代数の候補 |
| `faer` | 1.4M | 0.24.4 | 2026-06 | pure Rust 高性能密行列 + `sparse` / `matrix_free`。密行列の線形代数・有限要素法の候補 |
| `approx` | 30.6M | 0.5.1 | 2026-02 | 数値 assert のデファクト |
| `proptest` | 43.1M | 1.11.0 | 2026-03 | 性質ベーステスト |
| `criterion` | 54.1M | 0.8.2 | 2026-02 | ベンチ（密行列の線形代数から先） |
| `rustfft` | 6.2M | 6.4.1 | 2025-09 | FFT と Monte Carlo の検証相手 |
| `rand` | 374M | 0.10.2 | 2026-07 | FFT と Monte Carlo |
| `statrs` | 8.5M | 0.19.0 | 2026-07 | 統計は「呼ぶ」もの |
| `argmin` | 0.79M | 0.11.0 | 2025-09 | 最適化で trait 設計を読む |
| `ode_solvers` | 0.21M | 0.6.2 | 2026-06 | 常微分方程式で API を読んでから自作 |
| `sprs` / `nalgebra-sparse` | 1.5M / 0.16M | — | 2026-07 / 2026-05 | 疎行列は集約していない |

**自作が正当と判断したもの**（デファクトが不在または死んでいる）:

| 領域 | 状況 |
|---|---|
| 根の探索 | `roots` 0.35M だが最終更新 2022-12 で停止 |
| 自動微分 | **デファクト不在**。`num-dual` 87k は前進モードのみ、`autodiff` 33k は 2023-03 停止 |
| 有限要素法 | **デファクト不在**。`fenris` は 90日 242 DL / 最終更新 2023。既製品を呼ぶ選択肢が無い |
| 準乱数（FFT と Monte Carlo） | `sobol_burley` 16k、`quasirandom` 78 と弱い |

**全体の読み**: `peroxide` 0.16M / `russell_lab` 2.7k と、「全部入りの数値計算パッケージ」系は伸びていない。Rust には MATLAB / scipy 相当の単一パッケージに寄せる流儀が根付いておらず、領域ごとに薄いクレートを組むのが実態。

## 出発点のリポジトリ調査（2026-08-08、GitHub API で実測）

`vini-fda/numerical-algorithms-rust` の fork を取りやめた根拠:

- 最終 push 2023-12-28。コミット10本すべてが 12/24〜12/28 の4日間に収まり、以後停止。star 2 / fork 0。デフォルトブランチは `master`
- 全54ファイル中、中身のあるファイルは4本のみ（`src/README.md` 4.2KB、Introduction 7.4KB、How computers represent numbers 6.4KB、Integers 2.7KB）
- 残り 48ファイルはすべて 10〜53 バイト＝`# 見出し` 1行だけ
- `Cargo.toml` が無く、`.rs` ファイルが1つも無い。"in Rust" を名乗るリポジトリに Rust コードがゼロ
